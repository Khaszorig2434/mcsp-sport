PORT=4000
NODE_ENV=development

# Option A — Railway / hosted Postgres (takes priority when set)
# DATABASE_URL=postgresql://user:password@host:port/dbname

# Option B — local Postgres (used when DATABASE_URL is not set)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mcsp_sport
DB_USER=postgres
DB_PASSWORD=postgres
